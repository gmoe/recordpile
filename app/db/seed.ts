import dataSource from './dataSource';
import { PileItem } from './PileItem';
import { User } from './User';

/**
 * This is a database seed for testing purposes. Run this
 * to generate a User with a bunch of PileItems in various
 * states of completion.
 */
(async () => {
})();
